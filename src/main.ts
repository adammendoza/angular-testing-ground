import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';

import { MainModule } from './app/hello-world/modules/main.module';

platformBrowserDynamic().bootstrapModule(MainModule);
